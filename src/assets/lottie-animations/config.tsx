import Loading from './loading.json';

const loadingTitle = {
  loop: true,
  autoplay: true,
  animationData: Loading,
  rendererSettings: {
    preserveAspectRatio: "xMidYMid slice",
  },
};

export { loadingTitle };